let i = 0 ;

do{
    if(i % 2 === 0 ){
        console.log(i);
    }
    i++;
}while(i < 10);

// == simple comparison 
// === strict comparison because instead of values it also compares the data types 